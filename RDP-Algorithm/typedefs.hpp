#ifndef TYPEDEFS_HPP
#define TYPEDEFS_HPP

#include <utility>
#include <array>

using VecD  = std::pair<double, double>;
using VecI  = std::pair<int, int>;
using LineI = std::array<int, 4>;

#endif // !TYPEDEFS_HPP
